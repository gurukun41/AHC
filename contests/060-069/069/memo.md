# AHC069 作業メモ

10日間の作業内容を時系列で残す。各回、実装内容・狙い・テスト条件・スコア・次に試すことを追記する。

## 運用上の注意

- `AGENTS.md` の生成AI利用ルールを守る。
- 解答プログラムを実行した後は、その結果を受けたコード変更を自動で続けず、次の明示的な指示を待つ。
- スコアには入力 seed、実行ファイル、計測条件を併記し、異なる条件の値を直接比較しない。

## 2026-07-31: 初期実装 (`init`)

### 問題の要点

- 50×50 の公園へ1000組がオンラインで到着する。
- 受け入れる場合、空いている芝生からちょうど `P_i` マスの4近傍連結領域を割り当てる。
- 領域の周長を `L` とするとコンパクト度は `C = 4√P/L` で、退去時の収入は `round(V_i C_i)`。
- 利用中グループは移動できるが、移動コストがかかり、料金計算には滞在中の最小コンパクト度が使われる。

### 現在の方針

移動を行わず、配置可能なグループをすべて受け入れるベースライン。

1. `owner[50][50]` で占有状態を管理する。
2. 受け入れたグループの `(T_i, i)` を min-heap に入れ、各到着時に `T_j < S_i` の領域を解放する。
3. 人数ごとに「長方形＋端数行または端数列」で作れる最小周長形状を事前計算する。
4. 二次元累積和を使い、空き領域へ置ける最小周長形状を探す。
5. コンパクト形状が置けない場合は、全ての空き連結成分を BFS し、先頭 `P_i` マスを割り当てる。
6. 移動数として毎ターン `0` を出力し、応答末尾で必ず flush する。

既存の `Scanner`、`Emitter`、型定義、`inside` をそのまま利用している。

### 合法性

- 池または占有済みのマスを候補から除外する。
- 長方形と端数部分は重複せず、辺を共有するため必ず4連結になる。
- BFS の pop 順の先頭 `P_i` マスは、各マスの親が先に含まれるため4連結になる。
- 最初の空き成分が小さくても、後続の全成分を調べる。
- 受け入れ時だけ退去 heap と座標一覧へ登録する。

### 計算量

- 退去管理: 1組あたり `O(log M)`。
- BFS: 1ターン `O(N^2)`。
- コンパクト形状探索: 最小周長候補数を `K` として1ターン `O(KN^2)`。各配置の空き判定は二次元累積和により `O(1)`。
- 形状一覧は `P=4..150` について開始時に一度だけ生成する。

### 検証結果

- GNU++17、`-O2 -Wall -Wextra -Wshadow` でコンパイル成功。
- 公式ローカルテスタ、`tools/in/0000.txt`:
  - 正常終了 (`exit code 0`)
  - 絶対スコア: `42,762,963`
  - 経過時間: `0.67 sec`（テスタ込み）
- この実行後、解答コードは変更していない。
- 記録時点の `main.cpp` SHA-256: `35492b8a3a52ffb76e8d78c899278f5b93b5db0cf6989663103891ca69285a0a`

### 現時点の課題と次の候補

- 価値、占有面積、滞在時間を使った受け入れ判断がなく、低効率なグループが将来の配置を塞ぐ。
- コンパクト形状の配置位置は最初に見つかった場所で、将来の空き領域や退去時刻を考慮していない。
- BFS フォールバック領域は合法だが、周長が大きくなる場合がある。
- 移動を一切使っていない。移動コストと料金低下を上回る場合だけ再配置する余地がある。
- 次回は複数 seed の基準スコアを記録してから、受け入れ基準または配置位置評価を一つずつ比較する。

## 2026-07-31: 利用中1組の再配置 (`relocation-v1`)

### 目的

通常配置では拒否する到着に対し、利用中の1組だけを移動することで空き領域を連結し、移動費と既存組の料金低下を差し引いても利益が正になる場合だけ受け入れる。

既存組を良い形へ移しても過去の最低コンパクト度は回復しないため、既存組自身の料金改善を目的とした移動は行わない。

### 追加した状態と計算

- `GroupState` に `active, T, V, P, max_perimeter, cells` を保持する。
- 初期配置時に周長を保存し、移動時は `max_perimeter = max(max_perimeter, new_perimeter)` とする。
- 料金と移動費は公式ツールと同じ丸めになるよう `__int128` で補正する。
- 再配置の即時利益を次で評価する。

```text
新規組の見込み料金
- 移動組の移動費
- 移動組の周長履歴悪化による料金低下
```

### 再配置探索

1. 従来の `find_region` で配置できる場合は移動しない。
2. 配置できない場合に空き連結成分を一度ラベル付けする。
3. 空き総数が `P_i` 未満なら、移動しても容量が増えないため拒否する。
4. 各利用中組 `j` について、次の解放後成分サイズを計算する。

```text
P_j + j の領域に隣接する相異なる空き成分サイズの合計
```

5. この値が `P_i` 未満の組と、新規組の基本額が移動費以下の組を除外する。
6. 移動費が小さい順に最大16組を仮盤面で試す。
7. `j` の旧領域を仮盤面から消し、新規組を予約してから `j` の移動先を探す。
8. 新規領域は盤面の4方向から探したコンパクト領域と BFS 領域を候補にする。
9. 即時利益が正の候補から最大のものを採用する。

失敗した探索は仮盤面のみを変更する。計画確定後、実盤へ「全移動元の解放 → 全移動先 → 新規組」の順で反映する。

### 現在の制限

- 同時に移動するのは最大1組。
- 各移動候補について試す新規領域は最大5個。
- 移動候補は移動費順の先頭16組に制限している。
- 過去コードの `steady_clock` ベースの `Timer` を再利用し、開始から1.75秒以降は再配置探索を行わない。
- 即時利益だけを評価し、新規組が将来の到着を阻害する機会損失はまだ考慮していない。

### 静的検証

- 料金、移動費、履歴最大周長の計算が公式ツールと一致することを確認した。
- 仮盤面上で移動先と新規領域を互いに予約するため、固定組や相互の重複は発生しない。
- GNU++17、`-O2 -Wall -Wextra -Wshadow` でコンパイル成功。

### 公式テスタ実行結果

- 入力: `tools/in/0000.txt`
- 正常終了 (`exit code 0`)
- 絶対スコア: `45,074,775`
  - 初期実装: `42,762,963`
  - 差分: `+2,311,812`（約 `+5.41%`）
- 受け入れ: `889` 組
- 拒否: `111` 組
- 再配置: `113` 回
- 再配置時に見積もった即時利益の合計: `9,414,163`
- 経過時間: `1.53 sec`（テスタ込み）
- 実行した `main.cpp` SHA-256: `2998528c2aa25833bc1c2994d7ca440d1d1310876db146f236503c732dd3125a`
- この実行後、解答コードは変更していない。

## 2026-07-31: 退去時刻を考慮した通常配置 (`departure-aware-v1`)

### 狙い

次に到着するグループの情報は未知だが、現在利用中の各組の退去時刻は既知である。そこで、同じ料金・同じ周長の通常配置候補について、今後の退去で大きな空き連結成分が生まれやすい候補を選ぶ。

### 解放時刻と未来評価

各芝生マスに次の解放時刻を与える。

```text
現在の空きマス: S_i
既存組 j の占有マス: T_j
到着組 i の候補マス: T_i
```

- 現在の空きマスから DSU を作り、利用中グループを `T_j` 順に一組ずつ解放する。
- 各退去イベント後の最大空き成分を `L1`、第2空き成分を `L2` とし、`4 * L1 + L2` を空間価値とする。
  - 最大成分を150マスで打ち切ると、300マスの1成分より150マスずつの2成分を高く評価する逆転が起きるため、`L1` は飽和させない。
  - この式は空き成分同士が連結したときに値が低下しない。
- 未知の次回到着までの時間を指数分布で近似し、平均間隔を `(100000-S_i)/(残り組数+1)` とする。各退去区間に次回到着が起きる確率を重みとして、空間価値を積分する。
- 到着組が `T_i` に退去した後は、どの候補を選んでも盤面が同じ状態へ戻るため、未来評価は `T_i` より前だけ行う。
- 同点時は、隣接する芝生マス間の解放時刻差の合計が小さい候補を選ぶ。`T_i` より後の差は候補位置と無関係なので、解放時刻を `T_i` で打ち切って計算する。

### 候補と優先順位

通常配置について、盤面の4方向から最初に見つかるコンパクト領域と BFS 領域を重複除去し、最大5候補を比較する。

```text
1. 現在の見込み利用料が高い
2. 周長が短い
3. 未来の空き空間価値が高い
4. 解放時刻の境界差が小さい
```

したがって、未来評価だけを理由に現在の利用料や周長を悪化させない。再配置の採否は従来どおり即時利益で決め、今回の未来評価は通常配置だけに適用する。

### 計算量と時間管理

- 候補ごとに全芝生マスの解放時刻をソートせず、利用中グループを退去時刻順に並べ、その占有マスを直接 DSU へ追加する。
- 未来評価は開始から1.20秒まで行い、以降は従来の配置へ戻す。
- 再配置探索の終了時刻を1.60秒へ早め、2秒制限に対する後処理時間を確保する。

### 静的検証

- 退去イベント直前の状態へ区間重みを掛け、解放後の状態を次区間へ使う順序を確認した。
- 新規候補は現在の空きマスだけに置かれるため、既存組の解放時に同じマスを二重に DSU へ追加しない。
- 未来評価の前に `groups[i].t` が設定済みであることを確認した。
- GNU++17、`-O2 -Wall -Wextra -Wshadow` で警告なくコンパイル成功。
- AddressSanitizer / UndefinedBehaviorSanitizer を有効にしたビルドも成功。

### 公式テスタ実行結果

- 入力: `tools/in/0000.txt`
- 正常終了 (`exit code 0`)
- 絶対スコア: `44,949,872`
  - `relocation-v1`: `45,074,775`
  - 差分: `-124,903`（約 `-0.28%`）
  - 初期実装: `42,762,963`
  - 初期実装との差分: `+2,186,909`（約 `+5.11%`）
- 受け入れ: `889` 組
- 拒否: `111` 組
- 再配置: `100` 回
- 未来評価を使えた通常配置: `576` ターン
- 従来の先頭候補から変更した配置: `200` 回
- 再配置時に見積もった即時利益の合計: `9,168,664`
- 解答プログラム内の経過時間: `1.425 sec`
- テスタ込みの経過時間: `1.83 sec`
- 実行した `main.cpp` SHA-256: `8f321d8b0136a2533224f769930235a79b10f4bbac7893e30af4b34a0f4e7fc6`
- この実行後、解答コードは変更していない。

## 2026-07-31: 再配置なし比較 (`departure-aware-no-relocation`)

### 実験条件

- `ENABLE_RELOCATION = false` とし、通常配置に失敗した到着では再配置を試さず、その組を拒否する。
- 退去時刻を使う通常配置候補の未来評価、候補数、評価順、1.20秒の時間上限は `departure-aware-v1` と同じ。
- 再配置コード自体は残し、定数を戻せば再び有効化できる形にする。
- 全ターンで移動数として `0` を出力するため、対話出力の形式は変わらない。
- 再配置探索の計算時間がなくなることで、1.20秒以内に未来評価できるターン数が増える可能性がある。このため再配置収益だけを除く厳密なA/Bではなく、「再配置なしの実動版」として診断値も比較する。

### 静的検証

- 再配置無効時も通常配置の確定、拒否、退去 heap、所有盤面の更新経路が変わらないことを確認した。
- GNU++17、`-O2 -Wall -Wextra -Wshadow` で警告なくコンパイル成功。
- AddressSanitizer / UndefinedBehaviorSanitizer を有効にしたビルドも成功。

### 公式テスタ実行結果

- 入力: `tools/in/0000.txt`
- 正常終了 (`exit code 0`)
- 絶対スコア: `44,273,381`
  - `departure-aware-v1`（再配置あり）: `44,949,872`
  - 差分: `-676,491`（約 `-1.51%`）
  - `relocation-v1`: `45,074,775`
  - 差分: `-801,394`（約 `-1.78%`）
  - 初期実装: `42,762,963`
  - 初期実装との差分: `+1,510,418`（約 `+3.53%`）
- 受け入れ: `866` 組
- 拒否: `134` 組
- 再配置: `0` 回
- 未来評価を使えた通常配置: `95` ターン
- 従来の先頭候補から変更した配置: `48` 回
- 解答プログラム内の経過時間: `1.715 sec`
- テスタ込みの経過時間: `2.13 sec`
- 実行した `main.cpp` SHA-256: `9450304fcff2b9a1a004f6da89e919d077010ae5c9aef9852624da6380fdc740`
- 再配置あり版の `576` ターンに対して未来評価が `95` ターンまでしか進まず、実行時間の揺れが比較へ強く混入した。この1ケース1回だけでは、スコア差の全てを再配置の有無には帰属できない。
- この実行後、解答コードは変更していない。

## 2026-07-31: 需要重み付き未来評価 (`future-v2`)

### 実験条件

- `ENABLE_RELOCATION = false` を維持し、`departure-aware-no-relocation` から未来評価だけを差し替える。
- 現在の見込み利用料が最大、かつ周長が最小の通常配置候補だけを未来評価する。
- 候補生成は従来どおり、4方向の first-fit コンパクト領域と BFS 領域の最大5個とする。

### 需要重み付きスロット評価

代表人数を次の8種類とする。

```text
4, 9, 16, 25, 36, 64, 100, 150
```

- 真の `P=4..150` を `sqrt(P)` 上で最も近い代表人数へ割り当てる。
- 問題の生成式から求めた `P` の確率質量に、期待料金の近似として `P` と最良コンパクト度を掛け、代表人数ごとの重みを作って総和1へ正規化する。
- 空き連結成分のサイズを `c_r`、代表人数を `p` とし、概算配置枠数を次で定義する。

```text
A_p = sum_r floor(c_r / p)
```

- 枠数の価値は、1枠目を `1`、2枠目を `1.25`、3枠目以上を `1.5` として飽和させる。
- 空きマス数を `F`、空きマス同士の無向隣接辺数を `E` とし、`0.08 * E/(2F)` を加える。スロット数が同程度なら、通路状より太くまとまった空間を優先する。
- DSU の成分結合時に8種類の `A_p` を差分更新し、空きセル有効化時に `F,E` を更新する。従来の `multiset` による最大・第2成分管理は使わない。

### 退去区間の重み

- 指数分布による「次の1組の到着確率」は廃止する。
- 現在時刻、既存組の退去時刻、到着組自身の退去時刻で区間を作る。
- 各区間について、端点の退去時刻と重複せず未来の組が到着できる整数時刻数 `max(0, next_time-current_time-1)` を重みにする。
- 到着組の滞在中に複数回ある将来到着機会を近似するため、区間重み付き空間価値の平均を未来スコアとする。
- `T_i` 以降はどの候補でも元の空き盤面へ戻るため評価しない。

### 時間管理と診断

- 壁時計は候補方策の通常の切り替えには使わず、1.20秒を超えた場合の緊急停止だけに使う。
- 同率候補を全て評価できなかったターンでは部分結果を捨て、料金・周長最良の先頭候補へ戻す。
- 緊急停止後は従来の `find_region` だけを使う。
- `future_attempts`, `future_turns`, `future_candidates`, `future_emergency_stops`, `future_last_turn`, `changed_placements` を標準エラーへ出す。

### 静的検証

- DSU の結合ごとに `floor((a+b)/p)-floor(a/p)-floor(b/p)` がスロット数へ加算されることを確認した。
- 空き辺は後から有効化した端点で一度だけ数え、同じDSU成分内の辺も形状項へ含める。
- 退去前状態へ直前区間の重みを掛けた後、一組の全セルを有効化して次区間の状態を確定する。
- `T_j >= T_i` の既存組と候補自身の `T_i` 解放は評価に含めない。
- 3系統の静的レビューで、DSU増分、イベント境界、候補選択、緊急停止経路に修正必須事項がないことを確認した。
- GNU++17、`-O2 -Wall -Wextra -Wshadow` で警告なくコンパイル成功。
- AddressSanitizer / UndefinedBehaviorSanitizer を有効にしたビルドも成功。

### 公式テスタ実行結果

- 入力: `tools/in/0000.txt`
- 正常終了 (`exit code 0`)
- 絶対スコア: `45,511,141`
  - `departure-aware-no-relocation`: `44,273,381`
  - 差分: `+1,237,760`（約 `+2.80%`）
  - `departure-aware-v1`（再配置あり）: `44,949,872`
  - 差分: `+561,269`（約 `+1.25%`）
  - `relocation-v1`: `45,074,775`
  - 差分: `+436,366`（約 `+0.97%`）
  - 初期実装: `42,762,963`
  - 初期実装との差分: `+2,748,178`（約 `+6.43%`）
- 受け入れ: `860` 組
- 拒否: `140` 組
- 再配置: `0` 回
- future-v2 の候補列挙試行: `1000` ターン
- 同率候補を全て未来評価できたターン: `503` ターン
- 高コスト未来評価を完了した候補: `1618` 個
- 最後に未来評価を採用判断へ使った到着番号: `998`
- 料金・周長最良の先頭候補から変更した配置: `311` 回
- 緊急停止: `0` 回
- 解答プログラム内の経過時間: `0.395 sec`
- テスタ込みの経過時間: `0.63 sec`
- 実行した `main.cpp` SHA-256: `c32732fdd64cdcd3959da17483acbd49de7e3ed35fb33196a51507c334b00e0d`
- 旧再配置なし版より受け入れは6組減ったが、絶対スコアは増加した。
- この実行後、解答コードは変更していない。

## 2026-07-31: 全空き成分サイズ評価 (`component-size-v1`)

### 実験条件

- `future-v2` の需要スロット評価へ項を追加するのではなく、イベント時の空間価値を全空き連結成分のサイズ評価へ完全に置き換える。
- 代表人数、需要重み、概算配置枠数、空き隣接辺密度は評価から削除する。
- `ENABLE_RELOCATION = false`、候補生成、現在料金・周長の優先、退去区間の整数時刻重み、緊急停止条件は `future-v2` と同じ。

### 空間価値

ある退去区間の空き連結成分サイズを `c_1, c_2, ...`、空き総面積を `F` とし、空間価値を次で定義する。

```text
U = sum_k (c_k * c_k) / (F * F)
```

- `U` は、空きマスを独立に2個選んだとき同じ空き連結成分に属する確率に等しい。
- 全空きが1成分なら `1`、等面積の `K` 成分なら `1/K` となる。
- 全ての成分が二乗項として評価され、細かな分断より大きな連結成分を優先する。
- 同じイベント時点では候補間の空き総面積 `F` が等しいため、正規化による比較条件は公平である。
- 各退去区間の `U` を、未来の組が到着可能な整数時刻数で重み付けして平均する。

### DSU の増分更新

- サイズ1の空き成分を有効化すると `sum(c_k^2) += 1`。
- サイズ `a,b` の異なる成分を結合すると `sum(c_k^2) += 2*a*b`。
- 同じ成分内の辺では平方和を変更しない。
- `F=0` の場合は空間価値を0とする。
- `F<=2500` なので平方和は最大 `6,250,000` だが、積を含め `long long` で保持する。

### 静的検証

- slot、需要weight、空き辺密度に関する状態・関数・引数が残っていないことを確認した。
- DSU の平方和不変条件、二重結合防止、浮動小数点除算、退去イベント境界を3系統で静的レビューした。
- GNU++17、`-O2 -Wall -Wextra -Wshadow` で警告なくコンパイル成功。
- AddressSanitizer / UndefinedBehaviorSanitizer を有効にしたビルドも成功。

### 公式テスタ実行結果

- 入力: `tools/in/0000.txt`
- 正常終了 (`exit code 0`)
- 絶対スコア: `44,254,537`
  - `future-v2`: `45,511,141`
  - 差分: `-1,256,604`（約 `-2.76%`）
  - `departure-aware-no-relocation`: `44,273,381`
  - 差分: `-18,844`（約 `-0.04%`）
  - 初期実装: `42,762,963`
  - 初期実装との差分: `+1,491,574`（約 `+3.49%`）
- 受け入れ: `883` 組
- 拒否: `117` 組
- 再配置: `0` 回
- 候補列挙試行: `1000` ターン
- 同率候補を全て未来評価できたターン: `507` ターン
- 高コスト未来評価を完了した候補: `1652` 個
- 最後に未来評価を採用判断へ使った到着番号: `998`
- 料金・周長最良の先頭候補から変更した配置: `325` 回
- 緊急停止: `0` 回
- 解答プログラム内の経過時間: `0.392 sec`
- テスタ込みの経過時間: `0.80 sec`
- 実行した `main.cpp` SHA-256: `aaf6b516908f8a13a6dde653d53f25d46025936f5f77a8b9809e263d21569f85`
- `future-v2` より受け入れは23組増えたが、絶対スコアは低下した。
- この実行後、解答コードは変更していない。

## 2026-07-31: 提出候補を `future-v2` へ復元

- 最初の公式提出には、ローカル seed 0 で最高だった `future-v2` を使うことにした。
- `main.cpp` を需要重み付きスロット評価へ戻し、再配置は無効のままとした。
- 復元後の SHA-256 は `c32732fdd64cdcd3959da17483acbd49de7e3ed35fb33196a51507c334b00e0d`。
- この値は、絶対スコア `45,511,141` を記録した `future-v2` 実行時の SHA-256 と完全一致する。
- component-size版の実験内容と結果は上の節に履歴として残した。
- GNU++17、`-O2 -Wall -Wextra -Wshadow` で警告なくコンパイル成功。
- AddressSanitizer / UndefinedBehaviorSanitizer を有効にしたビルドも成功。
- 実測済みソースと完全一致しているため、解答プログラムの再実行は行っていない。
- この復元記録時点では、まだAtCoderへの提出操作は行っていない。

## 2026-07-31: 初回公式提出 (`future-v2`)

- 提出ソースの SHA-256: `c32732fdd64cdcd3959da17483acbd49de7e3ed35fb33196a51507c334b00e0d`
- 暫定50ケースの絶対スコア合計: `2,510,132,090`
  - 1ケース平均: `50,202,641.8`
- 実行時間: `407 ms`
- 記録時点の順位: `60 / 142`
- 記録時点のパフォーマンス表示: 水色
- 記録時点の相対スコア: `34,320,490,585`
  - 1ケース平均: 約 `686,409,812`
  - 50ケース全てで10億点を取る理論上限に対して約 `68.64%`
- コンテスト序盤で参加者数と各ケースの最大スコアが変動中のため、順位と相対スコアは暫定値として扱う。
- ローカル実行 `0.395 sec` に対して公式実行 `407 ms` で、時間面の再現性は良好だった。
- この提出結果を初回の公式ベースラインとして、以後の提出と絶対スコア合計を比較する。
- 提出後、解答コードは変更していない。

## 2026-07-31: 混雑時の受入制御 (`admission-v1`)

### 実験条件

- `future-v2` の候補生成、料金・周長の優先、退去時刻を使う未来評価は変更しない。
- `ENABLE_RELOCATION = false` を維持する。
- 通常配置候補が見つかった後、配置を確定する直前にだけ受入判定を行う。
- 拒否した場合は移動数 `0` と `No` を出力し、盤面、退去 heap、利用中グループへ登録しない。

### 収益密度と履歴

候補を受け入れた場合の見込み料金を `fee` とし、1マス・1時刻あたりの収益密度を次で定義する。

```text
density = fee / (P * (T-S))
```

- 現在の候補には、future-v2 が選んだ実際の周長から計算した `fee` を使う。
- 比較基準の履歴には、各到着を最小周長テンプレートへ置けたと仮定した潜在収益密度を使う。
- 受け入れ、空間不足による拒否、自発的拒否の全到着を履歴へ追加し、受入方策自身による基準値の偏りを避ける。
- 現在の到着は判定後に履歴へ追加し、判定では過去の到着だけを参照する。
- 下位25%点は、過去 `n` 件を昇順に並べた `floor(0.25*(n-1))` 番目とする。

### 自発的拒否の条件

次の4条件を全て満たしたときだけ、置けるグループを自発的に拒否する。

1. 過去の観測が64件以上ある。
2. 現在の占有マス数が全芝生マス数の75%以上である。
3. 現在組の滞在中に到着する未来組数の期待値が1以上である。
4. 現在候補の収益密度が、過去の潜在収益密度の下位25%点より小さい。

未来組数の期待値は、到着時刻が残り時間に一様に分布すると近似して次で求める。

```text
expected_arrivals = (M-i-1) * (T-S) / (100000-S)
```

- 占有率は、時刻 `S` より前に退去した組を除いた後、新規候補を置く前の値を使う。
- 最終組は未来組数が0となるため、この判定では拒否しない。
- `candidate_density == threshold` の場合は受け入れ、厳密に小さい場合だけ拒否する。

### 診断値

- `admission_considered`: 通常配置候補が存在し、受入判定を通った回数。
- `admission_pressure_turns`: 履歴、占有率、未来到着数の3条件を満たし、密度を比較した回数。
- `admission_rejected`: 自発的に拒否した回数。
- `admission_other_rejected`: 空間不足など、それ以外の拒否回数。
- `admission_rejected_fee`: 自発的拒否で見送った見込み料金の合計。
- `admission_rejected_cell_time`: 自発的拒否で解放した `P*(T-S)` の合計。
- 最終履歴の下位25%点、自発的拒否組の平均密度、拒否時閾値の平均も標準エラーへ出す。

### 静的検証

- `occupied_cells` は受入時に `P` を加算し、有効な組の退去時にその組の `P` を減算する。
- 占有率は退去処理後、今回組の配置前に評価する。
- 自発的拒否では空の `TurnPlan` を維持するため、盤面、グループ状態、退去 heap は更新されない。
- 現在組を履歴へ追加する処理は受入判定より後にあり、配置不能な到着も含め全ターンで1回だけ行う。
- `P*(T-S)` と見込み料金の集計は `long long`、密度と分位点は `long double` で計算する。
- future-v2 と再配置無効の既存処理は変更せず、通常候補確定直前の分岐だけを追加した。
- GNU++17、`-O2 -Wall -Wextra -Wshadow` で警告なくコンパイル成功。
- AddressSanitizer / UndefinedBehaviorSanitizer を有効にしたビルドも成功。
- 実行前の `main.cpp` SHA-256: `0dcd26df89dc8eff58aef0d0b3d9d0a93d01fd1c0df613a682416e7b96607901`

### 公式テスタ実行結果

- 入力: `tools/in/0000.txt`
- 正常終了 (`exit code 0`)
- 絶対スコア: `45,005,310`
  - `future-v2`: `45,511,141`
  - 差分: `-505,831`（約 `-1.11%`）
- 受け入れ: `849` 組
  - `future-v2`: `860` 組
  - 差分: `-11` 組
- 拒否: `151` 組
  - 自発的拒否: `43` 組
  - 空間不足など、それ以外の拒否: `108` 組
  - `future-v2` の空間不足による拒否 `140` 組から、その他の拒否は `32` 組減少した。
- 受入候補が存在した回数: `892` ターン
- 履歴、占有率、未来到着数の条件を満たした回数: `117` ターン
  - そのうち密度条件で拒否した割合: 約 `36.75%`
- 自発的拒否で見送った見込み料金: `1,315,797`
  - 1組平均: 約 `30,600`
- 自発的拒否で解放した `P*(T-S)`: `6,991,381`
  - 1組平均: 約 `162,590`
- 最終履歴の潜在収益密度の下位25%点: `0.307553`
- 自発的拒否組の平均収益密度: `0.216185`
- 自発的拒否時の閾値の平均: `0.313268`
- 再配置: `0` 回
- future-v2 の候補列挙試行: `1000` ターン
- 同率候補を全て未来評価できたターン: `524` ターン
- 高コスト未来評価を完了した候補: `1670` 個
- 最後に未来評価を採用判断へ使った到着番号: `998`
- 料金・周長最良の先頭候補から変更した配置: `325` 回
- 緊急停止: `0` 回
- 解答プログラム内の経過時間: `0.399 sec`
- 実行した `main.cpp` SHA-256: `0dcd26df89dc8eff58aef0d0b3d9d0a93d01fd1c0df613a682416e7b96607901`
- 自発的拒否によって後続の配置可能性は改善したが、seed 0 では見送った現在料金を将来収益で補えず、future-v2 よりスコアが低下した。
- この実行後、解答コードは変更していない。

## 2026-07-31: 残り到着数に応じた受入制御 (`admission-v2`)

### 実験条件

- `admission-v1` から、収益密度と比較する履歴分位点だけを変更する。
- future-v2 の配置、履歴64件、配置前占有率75%以上、再配置無効は維持する。
- `admission-v1` の `expected_arrivals >= 1` という二値条件と固定下位25%点を廃止し、期待到着数を連続的に分位点へ反映する。

現在組の滞在中に到着する未来組数の期待値を `A` とする。

```text
A = (M-i-1) * (T-S) / (100000-S)
q = 0.25 * (1-exp(-A))
```

現在候補の収益密度が、過去の潜在収益密度の下位 `q` 分位点より厳密に小さい場合だけ自発的に拒否する。

- `A=1` では下位約15.8%、`A=2` では下位約21.6%、`A=5` では下位約24.8%を基準にする。
- 未来の到着機会が少ないほど見送りを抑え、多いほど `admission-v1` の下位25%基準へ近づける。
- `A=0` となる最終組は受入制御で拒否しない。
- 分位点の添字は従来どおり、過去 `n` 件に対して `floor(q*(n-1))` とする。
- 数値的に小さい `A` でも精度を保つため、`1-exp(-A)` は `-expm1l(-A)` で計算する。

### 診断値

- `admission_pressure_turns` は、履歴と占有率を満たし、未来組が1組以上残っていて動的分位点を比較した回数とする。
- 比較時の動的分位点 `q` の平均と期待到着数 `A` の平均を追加で標準エラーへ出す。
- 自発的拒否数、その他の拒否数、見送った料金、解放した cell-time、拒否組の密度と閾値は `admission-v1` と同じ定義で記録する。

### 静的検証

- 有効入力では `A>0` となり、動的分位点は `0<q<=0.25` の範囲に収まる。
- 未来組が残っていない場合は分位点計算より前に受け入れる。
- 非常に小さい正の `q` では、既存の経験分位点の定義どおり履歴の最小値を基準にする。
- 分位点計算は最大1000件の履歴をコピーして `nth_element` するため、全体でも十分に小さい。
- 履歴更新、占有数、拒否時の状態、future-v2 の候補選択、出力形式に変更がないことを独立レビューで確認した。
- GNU++17、`-O2 -Wall -Wextra -Wshadow` で警告なくコンパイル成功。
- AddressSanitizer / UndefinedBehaviorSanitizer を有効にしたビルドも成功。
- 実行前の `main.cpp` SHA-256: `8529e68297761aa54174c3e175617df0ea24023fdf85055c6489cded4e9e4940`

### 公式テスタ実行結果

- 入力: `tools/in/0000.txt`
- 正常終了 (`exit code 0`)
- 絶対スコア: `45,004,388`
  - `admission-v1`: `45,005,310`
  - 差分: `-922`（約 `-0.00205%`）
  - `future-v2`: `45,511,141`
  - 差分: `-506,753`（約 `-1.11%`）
- 受け入れ: `848` 組
  - `admission-v1`: `849` 組
  - 差分: `-1` 組
- 拒否: `152` 組
  - 自発的拒否: `44` 組
  - 空間不足など、それ以外の拒否: `108` 組
- 受入候補が存在した回数: `892` ターン
- 履歴、占有率、未来組ありの条件を満たした回数: `123` ターン
  - 自発的に拒否した割合: 約 `35.77%`
- 自発的拒否で見送った見込み料金: `1,316,719`
  - 1組平均: 約 `29,925`
  - `admission-v1` から `922` 増加した。
- 自発的拒否で解放した `P*(T-S)`: `6,996,713`
  - 1組平均: 約 `159,016`
  - `admission-v1` から `5,332` 増加した。
- 最終履歴の潜在収益密度の下位25%点: `0.307553`
- 自発的拒否組の平均収益密度: `0.215202`
- 自発的拒否時の閾値の平均: `0.310100`
- 比較時の動的分位点の平均: `0.238239`（約23.82%）
- 比較時の期待到着数の平均: `29.856652` 組
- admission-v1 との差は、到着番号334の1組を追加で拒否したことに対応する。
  - 入力: `S=32902, T=32945, P=124, V=1615`
  - 見込み料金: `922`
  - `P*(T-S)`: `5,332`
  - 収益密度: 約 `0.172918`
  - 期待到着数 `A`: 約 `0.426168`
  - 動的分位点 `q`: 約 `0.086748`（下位約8.67%）
- 再配置: `0` 回
- future-v2 の候補列挙試行: `1000` ターン
- 同率候補を全て未来評価できたターン: `524` ターン
- 高コスト未来評価を完了した候補: `1670` 個
- 最後に未来評価を採用判断へ使った到着番号: `998`
- 料金・周長最良の先頭候補から変更した配置: `325` 回
- 緊急停止: `0` 回
- 解答プログラム内の経過時間: `0.416 sec`
- 実行した `main.cpp` SHA-256: `8529e68297761aa54174c3e175617df0ea24023fdf85055c6489cded4e9e4940`
- 比較対象では期待到着数が十分大きいことが多く、平均分位点は上限25%に近かった。
- 二値条件を外したことで `A<1` の短期滞在を1組だけ追加で拒否し、その見込み料金 `922` と同額だけスコアが低下した。
- この実行後、解答コードは変更していない。

## 2026-07-31: 全8方式の Pahcer 100ケース比較

### 実験条件

- 対象は `init`, `relocation-v1`, `departure-aware-v1`, `departure-aware-no-relocation`, `future-v2`, `component-size-v1`, `admission-v1`, `admission-v2` の8方式とする。
- 各方式の単発検証時に作成した保存済み実行バイナリをそのまま使い、ソース再構築による差異を避ける。
- Pahcer 0.3.1で seed 0〜99の100ケースを実行する。
- ケース範囲、`threads=0`、公式tester、入出力先は既存の `pahcer_config.toml` と揃える。
- 各実行では `--no-compile` を指定し、保存済みバイナリを直接testerへ渡す。
- `--freeze-best-scores` を指定し、実行順による既存best scoreの更新を止める。
- rawスコア合計・平均に加え、全8方式の各seedにおける最大スコアを100とした共通相対スコアを実行後に再計算する。
- 時間打ち切りを持つ方式は `threads=0` の並列負荷により単発実行と経路が変わる可能性があるため、今回の8方式間だけを同一条件の比較として扱う。

### 保存バイナリ

| 方式 | バイナリ | SHA-256 |
|---|---|---|
| `init` | `/tmp/ahc069_base` | `d3224a7595b98370bbda166de6de9ad68fd6bfec8b228847aa5d23528540f2f3` |
| `relocation-v1` | `/tmp/ahc069_relocation` | `ed5009a111437571053540d0f72abb4726bc9be5c19c30d97a9d1e39d6154509` |
| `departure-aware-v1` | `/tmp/ahc069_departure` | `7b043e9fe391a74cb966f3d074857b2a69daec1d6857dbf22cabc3ee068f2566` |
| `departure-aware-no-relocation` | `/tmp/ahc069_no_relocation` | `f6ffeca40ff9c9a0a9f57eb7cf1bb2ab01cd62eba0685004eb586f9a67ebbdb9` |
| `future-v2` | `/tmp/ahc069_future_v2` | `9d38ce0859fafb5dacabd9d965d060226bbda74ad00f206800e3a24fd0700cc1` |
| `component-size-v1` | `/tmp/ahc069_component_size` | `642ae8f3a457993449522510a8498867fd4987ac0eae334a80b6db3dae379632` |
| `admission-v1` | `/tmp/ahc069_admission_v1` | `cdd4147dd2b33f613461cd635eaca5b96a3bb569bbdfa27e67fa35f10efafab6` |
| `admission-v2` | `/tmp/ahc069_admission_v2` | `441e4f167d2334bbbcfa9c3f6803ddec1575b1e9a5c3240cdcc5a623f71e0de2` |

### Pahcer実行結果

全方式100ケース中100ケースAC、WAは0件だった。共通相対平均は、各seedにおける8方式中の最大スコアを100として再計算した値である。最高seed数は同点を含む。

| 順位 | 方式 | 100ケース合計 | 1ケース平均 | 共通相対平均 | 最高seed数 | 最大実行時間 |
|---:|---|---:|---:|---:|---:|---:|
| 1 | `admission-v1` | `5,130,043,657` | `51,300,436.57` | `98.971956%` | 36 | `1.293 sec` |
| 2 | `admission-v2` | `5,129,815,528` | `51,298,155.28` | `98.955278%` | 48 | `1.237 sec` |
| 3 | `relocation-v1` | `4,788,187,579` | `47,881,875.79` | `93.594833%` | 12 | `2.224 sec` |
| 4 | `future-v2` | `4,768,478,145` | `47,684,781.45` | `93.055950%` | 9 | `1.455 sec` |
| 5 | `departure-aware-v1` | `4,732,245,999` | `47,322,459.99` | `92.537109%` | 6 | `2.031 sec` |
| 6 | `departure-aware-no-relocation` | `4,690,618,600` | `46,906,186.00` | `91.673019%` | 0 | `1.606 sec` |
| 7 | `init` | `4,669,404,688` | `46,694,046.88` | `91.116318%` | 0 | `0.707 sec` |
| 8 | `component-size-v1` | `4,633,194,636` | `46,331,946.36` | `90.613430%` | 2 | `1.232 sec` |

単独最高seed数は `admission-v1` 23、`admission-v2` 35、`relocation-v1` 12、`future-v2` 9、`departure-aware-v1` 6、`component-size-v1` 2だった。残り13 seedは全て `admission-v1` と `admission-v2` の同点最高だった。

### 主な比較

- `admission-v1` は `future-v2` より合計 `+361,565,512`、約 `+7.58%`。
- `admission-v1` は `init` より合計 `+460,638,969`、約 `+9.87%`。
- `admission-v2` は `admission-v1` より合計 `-228,129`、約 `-0.00445%`で、100ケースではほぼ同等だった。
  - `admission-v1` 勝ち: 34 seed
  - `admission-v2` 勝ち: 41 seed
  - 同点: 25 seed
  - `admission-v1` の勝ちseedにおける差分合計: `+16,021,978`
  - `admission-v2` の勝ちseedにおける差分合計: `-15,793,849`
  - `admission-v2` の勝数は多いが、`admission-v1` の勝ち幅がわずかに大きく、raw合計では `admission-v1` が首位になった。
- `relocation-v1` は非admission方式の中で最高で、`future-v2` より合計 `+19,709,434`。
- `component-size-v1` は `init` より合計 `-36,210,052`、約 `-0.78%`。
- seed 0だけではadmission方式が `future-v2` を約1.11%下回ったが、100ケース合計では逆に約7.58%上回った。

### 実行時間と再現性

- `relocation-v1` は30ケース、`departure-aware-v1` は5ケースでPahcer計測時間が2秒を超えた。他6方式は2秒超過がなかった。
- 時間打ち切りを持つ方式では、`threads=0` の並列負荷によって単発実行時と配置経路・スコアが変わったケースがある。
- したがって今回のraw合計と共通相対平均は、同じPahcer設定で連続実行した8方式間の比較値として扱う。
- Pahcerの保存先は `pahcer/summary.md` と `pahcer/json/result_20260731_225013.json` から `result_20260731_225248.json` の各 `bench-all-*` 実行結果。
- 一括実行後も `main.cpp` は変更しておらず、SHA-256は `8529e68297761aa54174c3e175617df0ea24023fdf85055c6489cded4e9e4940` のままである。

## 2026-07-31: 2回目公式提出 (`admission-v2`)

- 提出ソースのSHA-256: `8529e68297761aa54174c3e175617df0ea24023fdf85055c6489cded4e9e4940`
- 暫定50ケースの絶対スコア合計: `2,743,947,673`
  - 1ケース平均: `54,878,953.46`
- 実行時間: `353 ms`
- 記録時点の順位: `68 / 192`
- 記録時点のパフォーマンス表示: ぎりぎり青色
- 記録時点の相対スコア: `36,322,250,314`
  - 1ケース平均: 約 `726,445,006`
  - 50ケース全てで10億点を取る上限に対して約 `72.64%`

### 初回提出 (`future-v2`) との比較

- 絶対スコア: `+233,815,583`、約 `+9.31%`
  - 1ケース平均では `+4,676,311.66`
- 実行時間: `407 ms` から `353 ms` へ `54 ms` 短縮。
- 相対スコア: `+2,001,759,729`、表示値では約 `+5.83%`。
- 順位割合は `60/142` の約42.25%地点から、`68/192` の約35.42%地点になった。
- 参加人数と他参加者の提出によって順位・相対スコアの基準は変動しているため、これらは厳密な同条件比較ではない。
- 絶対スコアの改善方向は、Pahcer 100ケースで `admission-v2` が `future-v2` を約7.58%上回った結果と一致した。
- 提出後、解答コードは変更していない。

## 2026-07-31: 占有率と履歴64件条件を使わない受入制御 (`admission-v3`)

### 変更方針

- ユーザーの「到着率」は期待到着数 `A` ではなく、判定開始までに必要としていた到着履歴64件を指すものとして、拒否判断から次の2条件を削除する。
  - 現在の占有率が75%以上であること。
  - 過去の観測が64件以上あること。
- 期待到着数 `A` と動的分位点 `q=0.25*(1-exp(-A))` は `admission-v2` のまま維持する。
- 過去履歴が1件以上あり未来組も残っていれば、占有率にかかわらず、通常配置可能な候補の料金密度を過去履歴の下位 `q` 分位点と比較する。
- 候補の料金密度が閾値を厳密に下回る場合だけ見送り、同値なら受け入れる。
- 先頭の到着は比較できる過去履歴が空なので受け入れる。未来組が残っていない最終到着も、`A=0` となるため受入制御では拒否しない。
- 履歴には従来どおり、受入・拒否にかかわらず各到着の最小周長時の潜在料金密度を追加する。
- 配置選択は `future-v2` のまま、再配置は無効のままとする。

### 診断値

- 占有率の診断値を削除する。
- 過去履歴があり、未来組も残っていて、動的分位点と比較した回数を `admission_density_comparisons` として出力する。
- 動的分位点と期待到着数の平均は従来どおり出力する。
- 拒否数、見込み料金、占有セル時間、候補密度、比較閾値は従来どおり記録する。

### 静的検証

- 占有率閾値と占有マス数の追跡を削除し、履歴件数の判定は空かどうかだけにした。
- 期待到着数、到着時刻上限、動的分位点の計算と診断出力は維持した。
- 過去履歴が空なら受け入れ、1件以上かつ未来組が残るなら占有率に依存せず動的分位点と比較する。
- 現在到着の履歴追加は判定後のままで、全到着を1回ずつ記録する挙動に変更はない。
- GNU++17、`-O2 -Wall -Wextra -Wshadow` で警告なくコンパイル成功。
- AddressSanitizer / UndefinedBehaviorSanitizer を有効にしたビルドも成功。
- 実行前の `main.cpp` SHA-256: `2fdad1151f69a3f679927fb4b6b24351551c715ce6f8c10cd97e26f845cc8080`

### Pahcer 100ケース実行結果

- Pahcer 0.3.1、seed 0〜99、`threads=0`、公式testerで実行した。
- `--freeze-best-scores` を指定し、既存best scoreは更新していない。
- 100ケース全てAC、WAは0件。
- rawスコア合計: `5,532,259,578`
  - 1ケース平均: `55,322,595.78`
- 最大実行時間: `1.758 sec`
- seed 0: `49,935,493`
- 実行結果: `pahcer/json/result_20260731_233150.json`
- 実行時のコメント: `bench-admission-v3-no-occupancy-or-64-warmup`

既存方式との同一100 seed比較:

| 比較対象 | 対象合計 | 差分 | 改善率 | 勝敗 |
|---|---:|---:|---:|---:|
| `admission-v1` | `5,130,043,657` | `+402,215,921` | `+7.8404%` | 77勝23敗0分 |
| `admission-v2` | `5,129,815,528` | `+402,444,050` | `+7.8452%` | 78勝22敗0分 |
| `future-v2` | `4,768,478,145` | `+763,781,433` | `+16.0173%` | 78勝22敗0分 |

- `admission-v1` 比の差分中央値は `+5,846,508.5`。
- `admission-v2` 比の最大改善は seed 5 の `+15,498,156`、最大悪化は seed 67 の `-10,227,055`。
- seed 0でも `admission-v2` より `+4,931,105`、約 `+10.96%`だった。

9方式の各seed内最大スコアを100として共通相対値を再計算すると、`admission-v3` は平均 `96.208177%`で1位だった。

- `admission-v1`: `91.052080%`
- `admission-v2`: `91.044665%`
- 100 seed中75 seedで `admission-v3` が単独最高だった。
- Pahcer表示の相対平均 `105.601` は、freezeした過去best scoreを分母にして100を超えているため、9方式比較には使用しない。
- 並列負荷で時間打ち切りの経路が変わり得るため、今回の値は同じPahcer設定で得た方式間比較として扱う。
- 実行後も `main.cpp` は変更しておらず、SHA-256は `2fdad1151f69a3f679927fb4b6b24351551c715ce6f8c10cd97e26f845cc8080` のままである。

## 2026-07-31: 履歴0件から判定する受入制御 (`admission-v4`)

### 変更方針

- `admission-v3` に残っていた「過去履歴が空なら受入制御では拒否しない」という最後の履歴件数条件も削除する。
- 現在到着の最小周長時の潜在料金密度を、配置候補の受入判定より先に履歴へ追加する。
- これにより履歴は1組目の判定時点から必ず非空となり、1組目を含む通常配置候補があり未来組も残る全到着で同じ分位点計算を行う。
- 期待到着数 `A`、動的分位点 `q=0.25*(1-exp(-A))`、占有率を使わないこと、再配置無効、`future-v2` の配置選択は変更しない。
- 最終到着は従来どおり未来組が0なので、受入制御では拒否しない。

### 履歴の定義

- 判定時の履歴は「現在までに到着した全グループ」の潜在料金密度とする。
- 現在到着も過去到着と同じく、実際の配置成否や受入結果に依存しない最小周長時の潜在料金密度を使う。
- 最終的な履歴内容は `admission-v3` と同じで、現在到着を追加するタイミングだけを判定後から判定前へ移す。

### 静的検証

- 履歴の空判定・件数判定は受入関数から完全に削除した。
- 潜在料金密度の履歴追加はループ内の1か所だけで、配置探索より前に無条件で行うため、各到着が配置成否にかかわらずちょうど1回追加される。
- 受入判定が呼ばれる時点では現在到着が追加済みなので、経験分位点へ空の配列が渡らない。
- 履歴1件では分位点添字が必ず0となる。最小周長候補なら現在の潜在密度と同値で受け入れ、周長が悪化した候補は1組目でも拒否され得る。
- 期待到着数、動的分位点、最終到着の受入、盤面状態と出力形式は `admission-v3` から変えていない。
- GNU++17、`-O2 -Wall -Wextra -Wshadow` で警告なくコンパイル成功。
- AddressSanitizer / UndefinedBehaviorSanitizer を有効にしたビルドも成功。
- 実行前の `main.cpp` SHA-256: `78bff6cbf4f2a9007cb4402c6eb7c9c3c8792173b8549f19521d06cfd2276350`

### Pahcer 100ケース実行結果

- Pahcer 0.3.1、seed 0〜99、`threads=0`、公式tester、`--freeze-best-scores`で実行した。
- 100ケース全てAC、WAと2秒超過は0件。
- rawスコア合計: `5,489,253,623`
  - 1ケース平均: `54,892,536.23`
- 最大実行時間: `1.927 sec`
- seed 0: `50,434,158`
- 実行結果: `pahcer/json/result_20260731_233651.json`
- 実行時のコメント: `bench-admission-v4-no-history-warmup`

| 比較対象 | 対象合計 | 差分 | 改善率 | 勝敗 |
|---|---:|---:|---:|---:|
| `admission-v3` | `5,532,259,578` | `-43,005,955` | `-0.7774%` | 39勝61敗0分 |
| `admission-v2` | `5,129,815,528` | `+359,438,095` | `+7.0068%` | 74勝26敗0分 |
| `future-v2` | `4,768,478,145` | `+720,775,478` | `+15.1154%` | 76勝24敗0分 |

- `admission-v3` 比の差分中央値は `-319,891.5`、第1四分位は `-1,232,957`、第3四分位は `+508,713`。
- `admission-v3` 比の勝ち差合計は `+39,991,326`、負け差合計は `-82,997,281`。
- `admission-v3` 比の最大改善は seed 76 の `+3,438,216`、最大悪化は seed 54 の `-5,052,748`。
- seed 0では `admission-v3` より `+498,665`、約 `+1.00%`だったが、100 seed合計では逆に悪化した。

10方式の各seed内最大スコアを100として共通相対値を再計算した結果:

- `admission-v3`: `95.705818%`、49 seedで単独最高。
- `admission-v4`: `94.989697%`、26 seedで単独最高。
- raw合計と共通相対平均の両方で `admission-v3` が1位、`admission-v4` が2位だった。
- 最大実行時間は `admission-v3` より約 `0.168 sec`長く、2秒まで約73 msだった。ただし `threads=0` の別バッチ実行なので、この時間差を方式変更の因果とは断定しない。
- 実行後も `main.cpp` は変更しておらず、SHA-256は `78bff6cbf4f2a9007cb4402c6eb7c9c3c8792173b8549f19521d06cfd2276350` のままである。

## 2026-07-31: `admission-v4` と1組再配置 (`admission-v4-relocation`)

### 変更方針

- `admission-v4` の受入制御、履歴、配置候補評価は変更せず、既存の1組再配置を有効化する。
- 通常配置候補が存在しない到着に限り、利用中の1組を別の空き領域へ移して到着領域を作れるか試す。
- 移動対象は最大16組まで調べ、解答プログラム開始から1.60秒に達した後の探索単位境界で再配置探索を打ち切る。
- 到着組の見込み料金から移動料金と既存組の料金低下を引いた即時利益が正の場合だけ再配置する。
- 再配置を伴う到着は、通常配置候補が存在しなかった場合に限られるため、現行コードではadmission判定を通さない。
- 1回の到着で移動する既存組は最大1組とする。

### 静的検証

- 解答コードの変更は `ENABLE_RELOCATION` を `false` から `true` にした1行だけで、受入制御と配置評価は変更していない。
- 到着領域が移動元を使うことと、到着領域を予約した後に既存組の合法な移動先が存在することを確認してから採用する。
- 盤面では移動元を消去してから移動先と到着領域を置き、既存組の最大周長、到着組の状態、退去heapを更新する。
- 出力は移動数1、移動組番号と移動先、`Yes` と到着領域の順で、失敗時は移動数0と `No` になる。
- 1.60秒は探索単位間で確認するソフトな打ち切りであり、単一の領域探索中に超える可能性がある。`admission-v4` の100 seed最大時間が1.927秒だったため、並列計測時の時間余裕には注意する。
- future評価と再配置は同じ全体timerを参照するため、再配置の計算時間によって後続ターンでfuture評価が無効になる時点も変わり得る。
- GNU++17、`-O2 -Wall -Wextra -Wshadow` で警告なくコンパイル成功。
- AddressSanitizer / UndefinedBehaviorSanitizer を有効にしたビルドも成功。
- 実行前の `main.cpp` SHA-256: `47fe9e77caee44efadebafa32e59a5c9ff95c1c6b9ad5dc45166bd9b476f4e82`

### Pahcer 100ケース実行結果

- Pahcer 0.3.1、seed 0〜99、`threads=0`、公式tester、`--freeze-best-scores`で実行した。
- 100ケース全て正常終了、WAは0件。
- rawスコア合計: `5,450,263,187`
  - 1ケース平均: `54,502,631.87`
- seed 0: `51,379,385`
- 実行結果: `pahcer/json/result_20260731_234321.json`
- 実行時のコメント: `bench-admission-v4-relocation`

| 比較対象 | 対象合計 | 差分 | 改善率 | 勝敗 |
|---|---:|---:|---:|---:|
| `admission-v4` | `5,489,253,623` | `-38,990,436` | `-0.7103%` | 40勝47敗13分 |
| `admission-v3` | `5,532,259,578` | `-81,996,391` | `-1.4822%` | 41勝58敗1分 |
| `admission-v2` | `5,129,815,528` | `+320,447,659` | `+6.2468%` | 76勝24敗0分 |

- `admission-v4` 比の差分中央値は `0`、第1四分位は `-1,109,195.5`、第3四分位は `+332,410`。
- 勝ちseedの差分合計は `+33,954,778`、負けseedの差分合計は `-72,945,214`。
- 最大改善は seed 44 の `+2,653,212`、最大悪化は seed 24 の `-5,866,498`。

保存済み標準エラーの再配置診断:

- 再配置が発生したケース: 74 / 100
- 再配置成功: 合計 `2,328` 回、1ケース平均 `23.28` 回、最大 `59` 回。
- 見積即時利益合計: `319,210,450`
- future評価の緊急停止: 57ケース。
- 再配置が発生した74ケースだけの `admission-v4` 比実スコア差は合計 `-43,375,504`。
- 見積即時利益は各再配置時点の局所値であり、再配置後の将来配置と計算時間によるfuture打ち切り経路の変化を含む最終スコア差とは一致しない。

11方式共通相対値では、`admission-v3` が `95.496913%`で1位、`admission-v4` が `94.782020%`で2位、`admission-v4-relocation` が `94.277304%`で3位だった。再配置ありは17 seedで単独最高、1 seedで同率最高だった。

- Pahcer最大計測時間は `2.242 sec`で、seed 3と6が2秒を超えた。
- ユーザーから、先頭の数ケースは実行環境の仕様上やや長くなるため気にしなくてよいとの補足があった。2秒超過はいずれも先頭バッチ内なので、今回の方式評価では時間面の悪化として扱わない。
- 実行後も `main.cpp` は変更しておらず、SHA-256は `47fe9e77caee44efadebafa32e59a5c9ff95c1c6b9ad5dc45166bd9b476f4e82` のままである。

## 2026-07-31: 配置換えを再び無効化

- `admission-v4-relocation` の比較結果を受けたユーザーの指示により、`ENABLE_RELOCATION` を `false` へ戻した。
- 受入制御、現在到着を含む履歴、期待到着数による動的分位点、`future-v2` の配置評価は変更していない。
- 解答コードは既に100 seedを測定済みの `admission-v4` と完全に一致するため、再実行は行わない。
- 復帰後の `main.cpp` SHA-256: `78bff6cbf4f2a9007cb4402c6eb7c9c3c8792173b8549f19521d06cfd2276350`

## 2026-07-31: 3回目公式提出 (`admission-v4`)

- 提出ソースのSHA-256: `78bff6cbf4f2a9007cb4402c6eb7c9c3c8792173b8549f19521d06cfd2276350`
- 暫定50ケースの絶対スコア合計: `2,988,372,107`
  - 1ケース平均: `59,767,442.14`
- 実行時間: `365 ms`
- 記録時点の順位: `71 / 226`
- 記録時点のパフォーマンス表示: ぎりぎり青色
- 記録時点の相対スコア: `39,354,092,254`
  - 1ケース平均: 約 `787,081,845`
  - 50ケース全てで10億点を取る上限に対して約 `78.71%`

### 2回目公式提出 (`admission-v2`) との比較

- 絶対スコア: `+244,424,434`、約 `+8.91%`
  - 1ケース平均では `+4,888,488.68`
- 相対スコア: `+3,031,841,940`、表示値では約 `+8.35%`。
- 実行時間: `353 ms` から `365 ms` へ `12 ms` 増加。
- 順位割合は `68/192` の約35.42%地点から、`71/226` の約31.42%地点になった。
- 参加人数と各ケースの最高スコアが変動しているため、順位割合と相対スコアは厳密な同条件比較ではない。
- 絶対スコアの改善方向は、Pahcer 100ケースで `admission-v4` が `admission-v2` より約7.01%高かった結果と一致した。
- 提出時点で配置換えは無効である。
